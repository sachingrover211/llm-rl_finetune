Episode {{ episode_number }}:

Matrix used :
{{ matrix_string }}

The function value for the matrix is {{ reward }}.

Based on the reward values, please provide a new weight matrix that you think will help increase the function value. Please generate the new weight matrix in the same format as the previous weight matrix (including the bias value in the last row).
